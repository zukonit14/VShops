<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vshops">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VShops">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".OrderDescriptionActivity" />
        <activity android:name=".MyProductViewActivity" />
        <activity android:name=".OrderProductActivity" />
        <activity android:name=".NewUserDetailsActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">
        </activity>
        <activity android:name=".ShopActivity" />
        <activity android:name=".MyPurchasesActivity" />
        <activity android:name=".MyOrdersActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>